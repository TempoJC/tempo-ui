import { LabelProps } from './types';
/**
 * Component which shows a text value as a label.
 */
declare const Label: ({ className, iconLeft, iconRight, variant, testId, color, label, uppercase, }: LabelProps) => import("react/jsx-runtime").JSX.Element;
export default Label;
