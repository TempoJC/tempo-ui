import { AvatarProps } from './types';
/**
 * If an image is used it will be shown as avatar, otherwise the initials will be used.
 */
declare const Avatar: ({ className, imageUrl, initials, size, onClick, color, shape, type, testId, }: AvatarProps) => import("react/jsx-runtime").JSX.Element;
export default Avatar;
