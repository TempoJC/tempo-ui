import { TagProps } from './types';
/**
 * Component that shows a text value as a tag.
 */
declare const Tag: ({ className, onClick, onRemoveButtonClick, readOnly, label, disabled, color, variant, testId, uppercase, }: TagProps) => import("react/jsx-runtime").JSX.Element;
export default Tag;
